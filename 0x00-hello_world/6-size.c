#include <stdio.h>

/**
 * main - program that prints the size of various types of the computer compiler
 *
 * Return: 0 on success
 */

int main(void)
{
	printf("size of a char: %lu byte(s)", sizesof(char));
	printf("size of int: %lu byte(s)", sizesof(int));
	printf("size of h")
}
